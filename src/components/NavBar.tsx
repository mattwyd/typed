import { AppBar, Toolbar} from '@mui/material/';


function NavBar(){
    return (
    <>
        <AppBar>
            <Toolbar
            sx={{backgroundColor: 'purple'}}>
                typed
            </Toolbar>
        </AppBar>
    </>
)}

export default NavBar;